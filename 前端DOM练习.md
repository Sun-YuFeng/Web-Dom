# 前端DOM练习

## 第一周：基础入门（DOM 核心概念 + 基础操作）

### Day1：DOM 基础认知

1. 手写代码：获取页面中以下元素（要求写出 3 种以上方式）

   - id 为`app`的元素
   - 所有 class 为`item`的元素
   - 所有`<li>`标签中第 2 个元素

   ​

2. 解释：`document`和`window`的关系？`DOM`和`BOM`的区别？

### Day2：DOM 节点操作

1. 手写代码：

   - 创建一个`<div>`元素，添加 class 为`box`，设置文本内容为 “面试刷题”，并追加到`body`末尾
   - 移除页面中 id 为`old-box`的元素
   - 克隆 id 为`card`的元素（分别实现深克隆和浅克隆）

   ​

### Day3：DOM 属性操作

1. 手写代码：

   - 获取 / 设置 input 输入框的 value 值（原生 JS 方式）
   - 获取 img 标签的 src 属性，修改 alt 属性
   - 区分：`element.getAttribute('class')`和`element.className`的区别？
   - 给元素添加自定义属性`data-index="1"`，并获取该属性值

   ​

### Day4：CSS 样式操作

1. 手写代码：

   - 给 id 为`box`的元素设置行内样式：宽 100px、高 100px、背景红色
   - 通过 class 控制样式：定义`.active`类（颜色白色、字体大小 16px），给元素添加 / 移除 / 切换该类
   - 解释：`element.style`和`getComputedStyle(element)`的区别？

   ​

### Day5：事件基础

1. 手写代码：

   - 给按钮绑定点击事件（3 种方式），点击后控制台输出 “点击成功”
   - 给 ul 下的所有 li 绑定点击事件，点击后输出当前 li 的索引（基础版）
   - 解释：事件冒泡和事件捕获的区别？如何阻止冒泡？

   ​

### Day6：事件进阶

1. 手写代码：

   - 实现事件委托：给 ul 绑定点击事件，点击 li 时输出当前 li 的内容（面试高频）
   - 给按钮绑定点击事件，要求只能点击一次（两种实现方式）
   - 解释：`event.target`和`event.currentTarget`的区别？

   ​

### Day7：第一周复盘

1. 综合题：实现一个简易的待办事项添加功能

   - 输入框输入内容，点击 “添加” 按钮，将内容添加到 ul 中
   - 每个 li 右侧有删除按钮，点击删除当前 li
   - 要求：使用原生 DOM 实现，禁止使用框架

   ​

## 第二周：进阶应用（DOM 性能 + 事件 + 样式）

### Day8：DOM 性能优化

1. 手写代码：

   - 批量插入 1000 个 li 元素（要求优化性能，对比直接插入和文档碎片的区别）
   - 解释：什么是回流（重排）和重绘？如何减少回流？

   ​

2. 面试题：为什么频繁操作 DOM 会卡顿？给出 3 种以上优化方案。

### Day9：事件高级

1. 手写代码：

   - 实现自定义事件：定义`custom-click`事件，触发时控制台输出自定义数据
   - 实现防抖函数：给搜索框绑定输入事件，输入停止 500ms 后才执行查询逻辑
   - 实现节流函数：给窗口绑定滚动事件，每 200ms 只执行一次滚动处理逻辑

   ​

### Day10：样式进阶

1. 手写代码：

   - 获取元素的非行内样式（如`width`、`margin-top`）
   - 实现一个函数，判断元素是否可见（包括`display:none`、`visibility:hidden`、`opacity:0`、超出视口等情况）
   - 解释：CSS 优先级计算规则？如何通过 JS 修改优先级最高的样式？

   ​

### Day11：DOM 遍历与节点关系

1. 手写代码：

   - 遍历一个嵌套的 DOM 结构（如 ul>li>div>span），输出所有节点的标签名
   - 实现函数：获取一个元素的所有父元素，直到 body
   - 实现函数：获取两个元素的最近公共父元素

   ​

### Day12：表单操作

1. 手写代码：

   - 实现表单全选 / 取消全选功能（复选框）
   - 获取表单中所有输入框的内容（包括 input/select/textarea），并验证必填项
   - 阻止表单默认提交行为，改为 AJAX 提交（原生 XHR）

   ​

### Day13：DOM 加载

1. 手写代码：

   - 写出 3 种等待 DOM 加载完成后执行代码的方式
   - 解释：`DOMContentLoaded`和`load`事件的区别？
   - 实现：图片懒加载（原生 JS，滚动时加载可视区域内的图片）

   ​

### Day14：第二周复盘

1. 综合题：实现一个原生 JS 轮播图

   - 自动轮播，每隔 2 秒切换一张
   - 点击左右箭头切换图片
   - 底部小圆点切换对应图片
   - 鼠标悬停暂停轮播，离开继续
   - 要求：无框架，优化 DOM 操作性能

   ​

## 第三周：高频面试手撕题（核心考点）

### Day15：DOM 元素拖拽

1. 手写代码：实现原生 JS 拖拽功能

   - 给 div 元素添加拖拽功能，可在页面内任意拖动
   - 限制拖拽范围（只能在父容器内拖动）
   - 要求：处理`mousedown`/`mousemove`/`mouseup`事件，优化拖拽卡顿问题

   ​

### Day16：防抖节流实战

1. 手写代码：

   - 面试手撕版：实现通用防抖函数（支持立即执行、取消防抖）
   - 面试手撕版：实现通用节流函数（时间戳版、定时器版、组合版）
   - 实战：给窗口绑定 resize 事件，使用节流优化；给搜索框绑定 input 事件，使用防抖优化

   ​

### Day17：深拷贝（含 DOM 对象）

1. 手写代码：

   - 实现深拷贝函数，支持拷贝普通对象、数组、日期、正则，**重点处理 DOM 元素的深拷贝**
   - 解释：深拷贝 DOM 元素时需要注意什么？和普通对象的区别？

   ​

### Day18：无限滚动

1. 手写代码：实现原生 JS 无限滚动列表

   - 滚动到页面底部时，加载下一页数据（模拟接口请求）
   - 添加加载中状态，防止重复请求
   - 优化：节流处理滚动事件，判断是否到底部的精准计算

   ​

### Day19：模态框（弹窗）

1. 手写代码：实现原生 JS 模态框

   - 点击按钮弹出模态框，点击遮罩层 / 关闭按钮关闭
   - 模态框居中显示，适配不同屏幕
   - 禁止背景滚动（弹窗显示时），关闭后恢复
   - 支持 ESC 键关闭弹窗

   ​

### Day20：Tab 切换

1. 手写代码：实现原生 JS Tab 切换

   - 点击 Tab 栏切换对应内容
   - 支持默认选中第一个 Tab
   - 实现 Tab 切换动画（CSS 过渡）
   - 要求：使用事件委托优化事件绑定

   ​

### Day21：第三周复盘

1. 综合题：实现原生 JS 下拉菜单

   - 鼠标悬停显示下拉菜单，离开隐藏
   - 支持多级下拉菜单
   - 处理鼠标快速移入移出的防抖问题
   - 优化：点击页面其他区域关闭下拉菜单

   ​

## 第四周：综合实战 + 面试模拟

### Day22：表单验证组件

1. 手写代码：实现原生 JS 表单验证组件

   - 支持必填、手机号、邮箱、密码强度验证
   - 实时验证（输入时），错误提示实时显示
   - 提交时统一验证，有错误阻止提交
   - 要求：封装成可复用的函数

   ​

### Day23：滚动监听导航

1. 手写代码：实现滚动监听导航功能

   - 滚动页面时，高亮当前视口对应的导航项
   - 点击导航项，平滑滚动到对应区域
   - 处理导航栏吸顶效果（滚动到一定距离后固定）

   ​

### Day24：图片预览 + 上传

1. 手写代码：实现原生 JS 图片预览和上传

   - 选择本地图片，实时预览（FileReader）
   - 限制图片大小和格式
   - 显示图片上传进度（XHR.upload）

   ​

### Day25：倒计时组件

1. 手写代码：实现原生 JS 倒计时组件

   - 支持自定义结束时间
   - 实时更新时分秒
   - 倒计时结束后触发回调函数
   - 要求：封装成组件，支持多实例

   ​

### Day26：DOM 操作综合题

1. 手写代码：实现一个简易的富文本编辑器（基础版）

   - 支持加粗、斜体、下划线功能
   - 支持撤销 / 重做（简易版）
   - 要求：使用 execCommand 或原生 DOM 操作实现

   ​

### Day27：面试模拟题 1

1. 手撕代码：

   - 实现：判断一个元素是否在视口内（多种实现方式，包括 getBoundingClientRect）
   - 实现：平滑滚动到页面顶部
   - 解释：什么是事件委托？为什么要用？举实际场景说明

   ​

### Day28：面试模拟题 2

1. 手撕代码：

   - 实现：动态创建表格，支持添加行、删除行、编辑单元格内容
   - 解释：DOM 事件的三个阶段？如何修改事件触发顺序？
   - 优化：表格数据量大时，如何优化渲染性能？

   ​

### Day29：面试模拟题 3

1. 手撕代码：

   - 实现：原生 JS 实现一个简单的虚拟列表（只渲染可视区域内的列表项）
   - 解释：虚拟列表的核心原理？解决了什么问题？

   ​

### Day30：全月复盘 + 综合实战

1. 综合大题：实现原生 JS TodoList 进阶版

   - 基础功能：添加、删除、修改待办事项

   - 进阶功能：

     - 待办事项分类（全部 / 已完成 / 未完成）
     - 本地存储（localStorage），刷新页面数据不丢失
     - 拖拽排序待办事项
     - 批量删除已完成事项

     ​

   - 性能优化：减少 DOM 操作，防抖 / 节流处理高频事件